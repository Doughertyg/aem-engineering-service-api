import { MAX_NUMBER_INPUT, MIN_NUMBER_INPUT } from './constants';

export const ERROR_MESSAGES = {
  INVALID_INPUT: `Error invalid input: Must provide a valid integer between ${MIN_NUMBER_INPUT} and ${MAX_NUMBER_INPUT}`,
};
